---
name: script-visual-director
description: 对剧本或简短情节进行电影化二次创作，形成叙事与心理分析、文字分镜、场景布局，以及角色、场景、道具参考图资产包。适用于 AI 视频前期视觉资产制作；不负责生成分镜首尾帧、配音或最终视频。
---

# 剧本视觉导演

根据用户的剧本建立可复用的视觉资产库和可审核分镜，供 MiniMax-H3 或其他视频工作流使用。文字分析由 Codex 完成，图片由实际可调用的图像生成工具制作，Python 工具负责布局、白模、文件与依赖校验、版本和打包。仅运行 Python 不会自动完成剧本理解或生成 AI 图片。

## 开始工作

将本文件所在目录记为 `SKILL_ROOT`，用户指定的资产输出目录记为 `PROJECT`；两者不同。遵守当前工作区的输出目录要求。阅读 [操作与数据约定](references/asset-schema.md)。若 `.venv` 不存在，按该文件创建项目虚拟环境后安装锁定依赖；不要向系统 Python 安装依赖。

使用 `SKILL_ROOT/.venv/bin/python SKILL_ROOT/scripts/svd.py` 调用工具；Windows 使用 `.venv/Scripts/python.exe`。所有后续相对输入路径均相对于资产项目，文件路径包含空格时正常引用。

用户只有一句描述也可开始：保留原文，标记 `explicit`、`inferred`、`proposed`。缺少风格时先提出代表方案；会改变剧情、血缘或已确认设定的信息才集中提问，不逐项追问普通设计细节。

## 剧集与后续集

用户提供第二集或系列续集时，先阅读 [跨集资产继承](references/series-continuity.md)。定位剧集共享库与上集资料，列出本集直接复用／参考改造／新增资产及确切版本，写跨集连续性说明，再进行本集二次创作。已确认的同造型人物四图和同状态道具可复用；场景须绑定本集布局、重新核对机位与状态。用 series-publish 将已完成图片入库，用 series-inherit 复制并锁定本集所需版本；不继承上集分镜和阶段批准，不自动升级已锁定素材。共享库或上集真实图片缺失时明确待补，不凭空宣称继承完成。

## 工作顺序与确认

1. 收集目标视频系统、具体模式/版本、画幅和目标时长；图像生成工具独立选择。目标未定时可先做通用分析与分镜。按目标官方指南适配格式；没有专用语法时使用推荐提示词结构，不自称官方格式。MiniMax-H3 只是适配目标之一。
2. 先阅读 [二次创作](references/creative-development.md)，将简单剧本丰富为可连贯阅读的中文改编场景：主动设计有作用的生活细节、潜台词、反应、画外信息与揭示方式，例如局部逐步揭示整体，不套固定特写数量。保留原文，标记提案；按用户已授权范围创作，核心事实改动随改编稿集中确认。先设计导演组的完整情绪起伏与创作时长，再拆镜头和技术任务。阅读 [叙事与摄影](references/narrative-and-camera.md)，分析信息投放、反应与缓冲的节奏，建立角色目标、认知、对人策略和关系变化；把心理转为有因果的可见表演，标记推断和设计提案。剧情分析文件登记后即运行 review-serve 并打开项目页；此时可以尚无分镜，不提前批准或编造镜头。让用户在同一页查看分析，再确认剧情理解及必要改编。
3. 按 [三项分镜规范](references/three-part-storyboard.md) 编写中文导演稿，每格正文仅呈现“场景 → 人物运动 → 镜头运动”。心理、节奏等保留为内部创作依据；先定镜头再安排动作是构思与可拍性检查原则，不再作为最终展示顺序。人物表演按“正在经历什么 → 顺势的小动作 → 少量细微反应”展开，细节由处境、人物习惯与关系触发且在当前取景内可见；不限定动作种类，不套固定微表情清单。明确动作起止、心理、可见表演、逐句台词语气与节奏，再生成目标系统分镜及逐镜中文对照。新项目用完整导演稿初始化，不能只导入摘要。初始化 [交互审核](references/interactive-review.md)，默认打开本地 HTML 审阅页确认；仅在用户明确选择纯对话或浏览器不可用时使用对话审阅。资产未生成时是待绑定适配稿；不得声称可直接运行。同步后按 [导演检查](references/director-review.md) 核对全段节奏、人物差异、表演与口气、时间、衔接及目标适配；通过后提交用户确认。已同步不等于导演检查通过。按 [导演组与任务](references/generation-groups.md) 建立“导演组 → 视频段生成任务 → 镜头”。导演组对应一次合并输出，可以53秒、48秒或更长；H3参考流程的15秒预算只检查内部每个生成任务，不限制导演组。JSON旧字段 groups 表示任务，新增 director_groups 表示导演组，不能混用。保留创作节奏、各段起点和前后承接，再编译每个任务的完整提示词及中文对照；确认两层方案后制作布局。目标导演台可替换，也可按通用包手动逐段生成与剪辑。
4. 阅读 [空间规划](references/spatial-planning.md)，按已确认分镜制作平面图、机位与人物路线，提交用户确认。若布局反证分镜不成立，修订受影响分镜并重新确认。
5. 布局确认后按 [本地预演](references/local-previs.md) 询问“生成预演／跳过预演”，用户已明确选择则沿用。复杂走位、运镜或遮挡应说明建议，但用户也可选择跳过；记录原话与未验证范围后继续资产流程，不把跳过记为通过。选择生成时阅读 [白模](references/blockout.md)，检测本机 Blender，编写本剧动画适配，按逐镜交付协议在后台生成每镜审核 MP4、纯摄影机参考 MP4、整段串联版、.blend 和帧序列；一镜对应一组文件，共用场景时间线并核对跨镜状态；先少量帧检查，再完整渲染，实际检查后提交用户确认。按确认任务输出短参考预演，另按导演组输出完整段落的审核串联版；导演组合并预演不作为单次H3输入。组内有缺镜时明确待完成，不拼成假完整组。页面默认只给状态与结果链接，按需打开视频。静态 glTF/PNG 或部分片段不能冒充完整预演；未装好时保存进度，安装后继续或交另一台机器运行。
6. 前置确认齐备后确定资产需求、所需角度与状态。一个镜头不必一张资产。先生成人物、场景、关键道具基准，确认后扩展。场景资产必须按 [预演到场景资产](references/scene-reference.md) 绑定布局版本、预演机位方向与几何形态，核对门窗、家具、通道和遮挡，不凭气氛另造空间。人物默认交付四张独立图：颈部以下正视、含头部背视、含头部左侧视、头部特写；共用同一人物基准，制作时阅读 [人物四图规格](references/character-reference.md)。家庭角色阅读 [家庭设计](references/family-design.md)，按亲缘依赖顺序制作；不是所有项目都需要家庭设计。
7. 阅读 [生图与检查](references/image-production.md)，用 queue 检查依赖后调用实际图像工具，查看结果并 register/review。approve 或 approve-gate 只记录真实用户依据，质量检查不替代批准。
8. 更改用 revise 保留旧版；中文分镜修改必须同步系统文本，检查邻镜和空间/资产影响。阅读 [交互审核](references/interactive-review.md) 处理“确认”“继续”“同步修改”，读取已保存进度，无须在同一对话重新调用 skill。跨对话按用户给定项目路径恢复。
9. 将确认后的真实图片顺序与版本绑定至目标格式，按当前模式重新核验，执行 validate --strict 后正式打包。MiniMax-H3 阅读 [H3 交接](references/h3-handoff.md)；其他系统同样验证实际导入方式。导演台交付另按 [生成分组](references/generation-groups.md) 绑定公共与组内素材、按任务检查输入限制，每个导演组独立执行 groups-package，或用 groups-bundle 输出全部独立导演包；不能把通用 manifest 冒充原生工作流。阶段包使用 --draft，明确未完成事项。

## 全流程审阅入口

HTML 审阅页贯穿分镜、分组、布局、预演、基准、扩展资产和交付，不在后半段退回只报文件路径。每次提交待确认内容或完成交付时，先登记真实文件与当前状态，再检查 `/api/state` 和相关文件可读，用当前环境的浏览器打开工具打开或刷新同一项目页面，并给出可点击的审阅 URL。工具不可用时明确说明，提供用户可打开的 URL 与启动命令；只有磁盘路径不算已展示审阅页。服务在运行所在机器的回环地址启动；换电脑必须在新机器启动，不能复用旧机器的端口和令牌。

用户回复“确认／继续”时先读取 review/state.json、分组状态与 manifest，沿已确认版本继续当前阶段；不要重建审核项目，也不要把完成预演当作完成全项目。每轮简短说明本轮结果、待确认对象及下一步；用户要求暂停资产时保留暂停。基准确认后继续四张独立人物图及所需场景／道具变体；导出后仍回到审阅页展示资产与交付文件。具体展示和恢复方式见 [交互审核](references/interactive-review.md)。

## 完成标准

- 二次创作的细节有叙事作用；局部到整体的揭示、人物反应与停留形成体验，不按技术预算机械切断段落，不用特写数量代替电影感。
- 角色在关键刺激之后的反应与选择符合其已知信息和目标；台词逐句带有合适口气，紧张段、反应段和缓冲各有作用，不机械堆砌快切或表情。
- 分析和资产能追溯到剧本或有理由的镜头需要；新增剧情单列建议。
- 已生成、已检查、已确认分别记录。依赖未就绪的资产不能进入批量生产。
- 输出真实可读的图片及分析、布局资料，路径相对且可迁移；参考基准一致。
- 未验证的三维条件、缺失工具或未知 H3 接口明确记录，不用图片占位、文字提示词或几何预览冒充最终视觉资产。
- 保存文件并给用户可用链接，说明本次完成内容及实际限制。

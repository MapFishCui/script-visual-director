---
name: script-visual-director
description: 对剧本或简短情节进行电影化二次创作，形成叙事与心理分析、文字分镜、场景布局，以及角色、场景、道具参考图资产包。支持按需单独生成参考图或目标系统提示词，也支持完整 AI 视频前期制作；不负责生成分镜首尾帧、配音或最终视频。
---

# 剧本视觉导演

根据用户的剧本建立可复用的视觉资产库和可审核分镜，供 MiniMax-H3 或其他视频工作流使用。文字分析由助手完成，图片由实际可调用的图像生成工具制作，Python 工具负责布局、白模、文件与依赖校验、版本和打包。仅运行 Python 不会自动完成剧本理解或生成 AI 图片。

## 运行环境

本文的“助手”指当前加载此 Skill 的 AI 助手，不限定平台。使用当前环境实际提供的文件、命令、联网、图像生成和展示工具，不假设存在特定工具名；按本次任务核实所需能力。缺少某项能力只说明受影响的交付，不声称已执行，也不因此阻断不依赖它的工作。平台专用安装配置不属于核心运行依赖。

## 先判断本次交付范围

按用户本次要求选择交付范围，不按输入剧本长短决定流程：

- 只要分镜、拍法或测试导演能力：使用“情节分镜”。
- 只要参考图、提示词，或参考图加提示词：使用 [按需交付](references/on-demand-delivery.md)。即使给了完整剧本，也不启动全流程。
- 明确要求全套制作，或继续既有完整项目的当前阶段：使用后面的完整流程。

“帮我生成人物图／资产”本身不等于要求完整项目。范围不明时按最小可用交付开始；只有不同理解会实质改变产物时才简短询问。回复“确认／继续”沿用当前范围，不能自动升级。按需交付与情节分镜优先于后面的项目初始化、阶段确认及全流程页面要求；仍遵守目标格式、真实素材、一致性和实际工具能力边界。

### 情节分镜：直接在对话中创作

这一路径在本次范围内优先于后面的项目初始化、文件登记、HTML 展示和阶段确认要求。无需创建 PROJECT、虚拟环境、manifest 或审核 JSON；不要求先确认分析，也不为了测试要求用户补写完整剧本。用户未指定系统、时长或画幅时，先按情节设计，不以收集制作参数阻断创作；只有影响核心剧情理解的歧义才需要澄清。

先读 [导演创作与取舍](references/directing-practice.md)，判断这一段在作品里的用途与分量，再用 [导演课程导航](references/director-course.md) 按实际难题选读。孤立的状态描述默认紧凑处理，不自动扩成完整事件链；用户明确要求展开或上下文需要时充分发展。先理解人物处境、观众信息和体验，再选择拍法；值得取舍时比较不同处理，不规定方案数量或镜头数量。不要把教材案例的动作、时间安排或揭示套路移植进新情节。镜头可以承担生活质感、关系、气氛和停留，不要求每镜都有剧情反转或微表情。

直接交付简短拍法说明与完整中文分镜。每镜正文按“场景 → 镜头运动 → 人物运动”写，摄影描述在动作前；明确谁在做什么、摄影机从哪里看及如何随动作变化，写观众实际能看见或听见的内容。台词按需要标注口气，估计时长仅供节奏参考。普通补充设计集中简述，保留原情节核心事实；不要输出十三个内部字段、七项通过表或自评分数。

交付前连续阅读画面和声音，检查人物是否可辨、观看过程是否成立、相邻镜头是否有无效重复，以及已知的空间与因果冲突；发现问题直接修改，不为初稿逐镜补辩护。用户修改时沿当前版本继续。只有用户要求系统提示词时才读取对应适配规范并编译；要求单项资产或提示词时转入按需交付；要求完整制作时才带入完整流程，补齐必要制作条件。不能把对话试写冒充已批准的生产记录。

## 完整项目：开始工作

将本文件所在目录记为 `SKILL_ROOT`，用户指定的资产输出目录记为 `PROJECT`；两者不同。遵守当前工作区的输出目录要求。阅读 [操作与数据约定](references/asset-schema.md)。若 `.venv` 不存在，按该文件创建项目虚拟环境后安装锁定依赖；不要向系统 Python 安装依赖。

使用 `SKILL_ROOT/.venv/bin/python SKILL_ROOT/scripts/svd.py` 调用工具；Windows 使用 `.venv/Scripts/python.exe`。所有后续相对输入路径均相对于资产项目，文件路径包含空格时正常引用。

用户只有一句描述也可开始：保留原文，标记 `explicit`、`inferred`、`proposed`。缺少风格时先提出代表方案；会改变剧情、血缘或已确认设定的信息才集中提问，不逐项追问普通设计细节。

## 剧集与后续集

用户提供第二集或系列续集时，先阅读 [跨集资产继承](references/series-continuity.md)。定位剧集共享库与上集资料，列出本集直接复用／参考改造／新增资产及确切版本，写跨集连续性说明，再进行本集二次创作。已确认的同造型人物四图和同状态道具可复用；场景须绑定本集布局、重新核对机位与状态。用 series-publish 将已完成图片入库，用 series-inherit 复制并锁定本集所需版本；不继承上集分镜和阶段批准，不自动升级已锁定素材。共享库或上集真实图片缺失时明确待补，不凭空宣称继承完成。

## 导演创作学习

开始新剧本时先读 [导演创作与取舍](references/directing-practice.md)，再用 [《导演必修课》阅读导航](references/director-course.md) 按当前问题选读。主动设计观众如何进入、期待、理解和离开场面，不等用户提供局部镜头技巧。先形成符合本段用途的连贯拍摄构想，再决定取景、剪辑和表演；三项字段及目标系统格式在方案之后，不要求所有段落都有完整起承转合。普通细节和拍法在既有授权范围内提出，核心事实改变沿用分析确认。

关键场面比较值得取舍的其他拍法；登场、对白、沉默、声音、空间、转场、揭示等按戏选择，不设镜数、运动比例或局部到整体公式。完稿后先遮住原有创作理由，仅按实际可见可听内容做观众立场的自评，发现问题再修订并同步；不要把复述原设计当审核证据，更不能批量复制字段并全部标 pass。

构想、真实修改与未验证范围放进现有分镜审阅说明及 sequence_note，随分镜一次审阅，不新增确认关卡。全场判断节奏与人物，逐镜检查适用问题；七项存档不是每镜七次创作论证，规范合格不等于创作有效。布局、预演和样片验证的范围应区分；教材学习不等于永久训练。更新 skill 或检查题材适应性时，可在隔离目录使用 [导演创作行为试题](references/directing-evaluation.md)，不把试写登记成生产成果。

## 工作顺序与确认

1. 收集目标视频系统、具体模式/版本、画幅和目标时长；图像生成工具独立选择。目标未定时可先做通用分析与分镜。按目标官方指南适配格式；没有专用语法时使用推荐提示词结构，不自称官方格式。MiniMax-H3 只是适配目标之一。选用 H3 时先读 [官方 skill 接入](references/h3-official-skill.md)，按素材实际用途路由模式；正文编译支持范围与官方能力分开说明。
2. 先阅读 [二次创作](references/creative-development.md)、[叙事与摄影](references/narrative-and-camera.md) 并形成剧情与角色依据。在审核页面编写用户所说的“中文导演稿”：逐镜的场景、镜头运动、人物运动，附时长、台词语气与必要创作依据；它已包含拍法提案，不是另加一份必需的散文稿。按 [创作与取舍](references/directing-practice.md) 先构思再整理字段，不为填表加戏。初始化 review-init --workflow layout-first-v1 并打开 review-serve；可先以空镜表展示进度，再用 review-storyboard 首次导入真实中文稿。先让用户确认这份中文导演稿，程序用 analysis 阶段记录确认并绑定正文版本；此时不要求先编译系统原文。三项正文遵守 [写作规范](references/three-part-storyboard.md)，摄影描述在人物动作之前。
3. 中文导演稿确认后，按 [平面图与白模流程](references/layout-first-workflow.md) 和 [空间规划](references/spatial-planning.md) 制作平面图，确定结构、物件、通道、人物初始位置并让用户确认。中文稿的摄影与行动为设计依据，空间不成立时回修中文稿。观察机位和不属于特定镜头的路线可用 shot=null。
4. 平面图确认后询问生成／跳过整场调度白模，用户已明确选择则沿用。按 [整场白模协议](references/layout-first-workflow.md) 用观察机位展示连续行动、事件顺序和物件交互，可辅以中文稿提出的摄影机视角；此时是在验证拍法与行动，不先锁定最终剪辑。blocking-* 命令与后面的逐镜预演分别存档，先诊断帧后完整 MP4，实际检查后让用户确认。白模精度以比例、关键结构、可辨认姿态、必要接触与遮挡为准，不只移动方块。 优先按 [现成三维资产库](references/model-library.md) 查找、下载留存与锁定合适的人物和物件；模型导入、视觉检查和用户批准分开记录，不默认重新拼人体。 需要把导演稿转成统一场景、行动和摄影机时，读取 [场面计划执行](references/scene-execution.md)，优先 scene-compile / scene-bind；缺失动作返回 blocked，不以默认站姿或平移替代人物表演。 可复用 [建模组件](references/modeling-components.md) 的结构、骨骼 IK、接触点与检查器，先读取其能力边界和坐标约定；失败须修正并重跑，不用笼统的“启用碰撞”代替几何验证。跳过须记录原话与未验证范围。
5. 白模确认或明确跳过后，核对中文导演稿与实际空间、行动，修订受影响内容，然后编译供目标系统执行的最终分镜及逐镜中文对照。场面可有省略、停留、不同观看距离，不把观察录像直接切段。H3 使用 [官方规范](references/h3-handoff.md) 及 scripts/h3.py，明确参考来源与用途；素材尚未生成或绑定时仍标为待绑定稿。按 [导演检查](references/director-review.md) 核对完整段落与转换，提交用户确认最终分镜。无需重新建立镜表或审核项目。按 [导演组与任务](references/generation-groups.md) 同时确认导演组及内部生成任务，H3 的单任务限制从任务规划起检查，不限制整场白模或导演组长度。
   最终分镜确认后，可按 [逐镜摄影机预演](references/local-previs.md) 输出各镜审核与纯摄影机 MP4，用户也可跳过。整场调度白模不直接冒充逐镜 H3 运镜参考。生成后再核对参考与最终文本；中文稿或空间、调度实质修改会使相关确认过期，单纯系统文本转换不会撤销上游中文稿与空间确认。
6. 前置确认齐备后确定资产需求、所需角度与状态。一个镜头不必一张资产。先生成人物、场景、关键道具基准，确认后扩展。场景资产必须按 [预演到场景资产](references/scene-reference.md) 绑定布局版本、预演机位方向与几何形态，核对门窗、家具、通道和遮挡，不凭气氛另造空间。人物默认交付四张独立图：颈部以下正视、含头部背视、含头部左侧视、头部特写；共用同一人物基准，制作时阅读 [人物四图规格](references/character-reference.md)。家庭角色阅读 [家庭设计](references/family-design.md)，按亲缘依赖顺序制作；不是所有项目都需要家庭设计。
7. 阅读 [生图与检查](references/image-production.md)，用 queue 检查依赖后调用实际图像工具，查看结果并 register/review。approve 或 approve-gate 只记录真实用户依据，质量检查不替代批准。
8. 更改用 revise 保留旧版；中文分镜修改必须同步系统文本，检查邻镜和空间/资产影响。阅读 [交互审核](references/interactive-review.md) 处理“确认”“继续”“同步修改”，读取已保存进度，无须在同一对话重新调用 skill。跨对话按用户给定项目路径恢复。
9. 核对已确认逐镜预演与分镜，逐镜说明各参考素材的用途与保留范围；将确认后的真实图片顺序与版本绑定至目标格式，按当前模式重新核验，执行 validate --strict 后正式打包。MiniMax-H3 阅读 [H3 交接](references/h3-handoff.md)；其他系统同样验证实际导入方式。导演台交付另按 [生成分组](references/generation-groups.md) 绑定公共与组内素材、按任务检查输入限制，每个导演组独立执行 groups-package，或用 groups-bundle 输出全部独立导演包；不能把通用 manifest 冒充原生工作流。阶段包使用 --draft，明确未完成事项。

## 全流程审阅入口

HTML 审阅页贯穿分镜、分组、布局、预演、基准、扩展资产和交付，不在后半段退回只报文件路径。每次提交待确认内容或完成交付时，先登记真实文件与当前状态，再检查 `/api/state` 和相关文件可读，用当前环境的浏览器打开工具打开或刷新同一项目页面，并给出可点击的审阅 URL。工具不可用时明确说明，提供用户可打开的 URL 与启动命令；只有磁盘路径不算已展示审阅页。服务在运行所在机器的回环地址启动；换电脑必须在新机器启动，不能复用旧机器的端口和令牌。

用户回复“确认／继续”时先读取 review/state.json、分组状态与 manifest，沿已确认版本继续当前阶段；不要重建审核项目，也不要把完成预演当作完成全项目。每轮简短说明本轮结果、待确认对象及下一步；用户要求暂停资产时保留暂停。基准确认后继续四张独立人物图及所需场景／道具变体；导出后仍回到审阅页展示资产与交付文件。具体展示和恢复方式见 [交互审核](references/interactive-review.md)。

## 完成标准

以下按本次交付范围适用；单项交付不要求补齐未请求的分析、布局、白模或其他资产。

- 二次创作的细节有叙事作用；局部到整体的揭示、人物反应与停留形成体验，不按技术预算机械切断段落，不用特写数量代替电影感。
- 角色在关键刺激之后的反应与选择符合其已知信息和目标；台词逐句带有合适口气，紧张段、反应段和缓冲各有作用，不机械堆砌快切或表情。
- 分析和资产能追溯到剧本或有理由的镜头需要；新增剧情单列建议。
- 已生成、已检查、已确认分别记录。依赖未就绪的资产不能进入批量生产。
- 输出真实可读的图片及分析、布局资料，路径相对且可迁移；参考基准一致。
- 未验证的三维条件、缺失工具或未知 H3 接口明确记录，不用图片占位、文字提示词或几何预览冒充最终视觉资产。
- 保存文件并给用户可用链接，说明本次完成内容及实际限制。

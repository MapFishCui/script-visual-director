# 剧本视觉导演：项目设计文档

版本：九宫格流程与交互审核修订（保留首版设计记录）
更新日期：2026-09-17
Skill 标识：`script-visual-director`

项目目录：`/Users/cuipeng/Work/Project/script-visual-director`

本文记录已讨论的产品范围及第一版实现。已实现 Skill 指令、Python 工具、项目内虚拟环境、SVG／PNG 平面图、自包含 glTF 三维白模、依赖与确认校验、版本修订和打包。具体机器字段与命令以 references/asset-schema.md 和 scripts/schema.py 为准。真实 AI 生图通过助手当前可调用的工具执行，本次开发测试没有生成生产图片，也未验证另一台电脑上的 MiniMax-H3 工作流。

## 1. 项目目标

用户提供完整剧本或简短情节描述，Skill 在助手中完成剧情理解、角色心理分析、空间规划、文字分镜设计和图片资产制作，交付可复制到另一台电脑、供 MiniMax-H3 后续处理的资产包。

当前默认流程：

**中文导演稿确认 → 人物基准与四视图合图确认 → 逐段九宫格完整关键画面确认 → H3提示词与中文对照确认 → 文件交付。**

平面图和白模按需辅助，不是默认前置关卡。运行协议见 references/keyframe-grid-workflow.md。人物参考图使用纯白背景，九宫格包含完整剧情环境；二者是视觉引导，不是硬锁帧。

保留的布局优先流程（已有项目或明确选择）：

**剧情理解 → 中文导演稿确认 → 平面图确认 → 整场调度白模确认或跳过 → 最终系统分镜及中文对照确认 → 可选逐镜摄影机预演 → 基准与扩展资产 → 绑定参考、重编译与打包。**

中文导演稿指页面中的逐镜三项中文方案；不是另加一篇必需的散文。布局优先项目运行协议见 references/layout-first-workflow.md，已有项目保留原记录。

空间与文字分镜设计允许往返调整。简单场景可以省略绘图，多机位或人物走位场景采用平面布局图，复杂高低差、立体遮挡和运镜按需采用三维白模。正式场景资产生成之前，应完成与其相关的布局和镜头检查。

Skill 的价值在于把叙事需要转化为可复用、可追踪的视觉资产，并控制人物、道具和场景在不同状态下的一致性。

## 2. 范围与边界

### 2.1 第一版包含

- 解析完整剧本、片段或简短描述，识别人物、关系、事件、地点和关键道具。
- 分析剧情节拍、紧张度、角色心理变化和观众的信息状态。
- 在用户未指定镜头时，主动设计文字分镜，包括景别、机位、表演、运镜和衔接。
- 具备绘制可编辑场景平面布局图的能力，按需标注人物走位、机位、视线和运镜路线，并导出预览。
- 判断是否需要三维白模；需要时通过可用三维工具建立简化几何与摄像机，验证空间、遮挡及运镜。该能力为按需模块，具体实现分阶段完成。
- 依据文字分镜补齐资产的视角、细节与状态需求。
- 建立视觉风格、家庭角色关系及外貌设定。
- 生成角色、场景、道具参考图，以及有明确需求的细节和状态变体。
- 检查一致性、记录版本、支持局部修改，生成资产索引和 ZIP 包。

### 2.2 第一版不包含（历史范围）

当前版本已扩展为生成完整关键画面九宫格与目标提示词，下列首版排除项中相应部分不再适用于新模式。当前仍不生成配音或最终视频，不承诺九宫格的各格被精确锁定。

- 分镜首帧、尾帧或逐镜头成片画面的自动生成。
- 配音、音乐、音效、最终视频生成和剪辑。
- 在另一台电脑上安装、部署或运行 MiniMax-H3。
- 默认生成可直接执行的 H3 视频任务、完整视频提示词或 ComfyUI 工作流。

文字分镜用于确定视觉表达和资产需求。手部、袖口、信封细节属于参考资产；“手在特定构图中拆信”的完整镜头画面不作为默认交付。交互参考图仅在用户明确需要时扩展。

平面图属于影视制作空间示意图，不作为施工图。白模包含准确比例、关键结构、可辨认的人物姿态与必要物件交互；影响行动或遮挡的轮廓应细化，可以使用必要的简化骨架与关节动画。最终面部、服装材质和口型不属于调度确认范围。白模机位预览属于空间验证资料，不是分镜首尾帧资产。

## 3. 输入与缺省处理

### 3.1 必需输入

一份剧本、情节片段或简短描述。

### 3.2 可选输入

| 输入 | 用途 |
| --- | --- |
| 视觉风格、参考作品描述或参考图 | 确定写实程度、时代、色调和材质表现 |
| 已确认的角色、场景、道具图片 | 作为后续生成的基础，避免重新设计 |
| 角色关系、年龄、外貌和服装要求 | 约束角色设计与家庭相似性 |
| 目标片段时长、画幅 | 辅助判断镜头节奏和资产构图 |
| 已有平面图、房间尺寸、现场照片或三维场景 | 约束布局；区分实际测量与设计假设 |
| 创作自由度 | 确定可补充设计的程度 |
| 生成数量、预算或重试上限 | 控制候选图数量与生成范围 |
| H3 版本和运行方式 | 为后续导入适配提供依据 |

缺省策略：先完成不依赖缺失信息的分析，给出一套有依据的建议。普通视觉细节作为明确标记的设计假设；会改变剧情、血缘关系、人物动机或重要信息揭示的缺失信息，不静默补成事实。生成代表样图前集中说明影响全局的风格和外貌假设。

H3 运行方式未知不阻塞叙事分析和通用资产制作，但不得把资产包标记为已通过目标工作流验证。

## 4. 叙事分析与自动文字分镜

### 4.1 信息依据

分析中的关键判断使用以下标记：

- `explicit`：原文明示，可关联原文段落。
- `inferred`：根据动作或上下文作出的合理推断。
- `proposed`：为视觉表达补充的设计，允许修改或移除。

没有依据时，不为角色添加创伤史、秘密身份、医学诊断或确定动机。称谓“爸爸、妈妈、儿子”不自动证明血缘关系。

### 4.2 剧情节拍与紧张度

按事件或心理转折拆分节拍，记录当前发生的事、信息变化、冲突、威胁、时间压力及悬念释放。紧张度可采用 1–5 级进行段落内比较，每个等级附理由；它是创作辅助判断，不是客观测量值。

紧张度不直接绑定摄影手法。低紧张段落可以有运动镜头，高紧张段落也可以使用固定长镜头。不得为平淡描述自动添加追逐、危险或惊吓。

### 4.3 角色心理

对相关角色记录：

- 当前目标与阻碍。
- 已知和未知的信息。
- 内心冲突及情绪的进入、变化、退出状态。
- 可见表达建议：眼神、姿态、动作速度、停顿等。
- 判断依据及不确定性。

内在心理不等于观众能直接看到的信息。外在表现需要符合剧情和人物，不要求每种情绪都有夸张表情。

### 4.4 文字分镜

用户未指定镜头时，主动提出一套克制、连贯的方案。每个镜头记录：

| 字段 | 内容 |
| --- | --- |
| 镜头编号、对应节拍 | 关联原文和分析 |
| 叙事目的 | 建立空间、突出情绪、展示动作或揭示信息 |
| 景别、机位和构图 | 说明镜头所见与主体关系 |
| 空间关联 | 场景与布局版本、机位编号、人物起止位置；未建布局时注明 |
| 人物动作和表演 | 采用可见、可执行的描述 |
| 运镜 | 运动起点、方向、停止条件及目的；可为固定镜头 |
| 时长建议 | 若有总时长则协调分配，否则标为暂定 |
| 衔接要求 | 视线、运动方向、位置、动作和持物状态 |
| 所需资产 | 关联资产编号及视角、细节、状态需求 |
| 依据类型 | 原文要求、推断或导演建议 |

镜头数量由内容决定。每个镜头应提供必要信息或情绪变化；复杂动作和运镜按可执行性拆分，不为了凑数增加镜头。用户指定的镜头优先。

## 5. 空间规划、分镜校验与资产需求

### 5.1 整体执行顺序

| 阶段 | 主要工作 | 产出 |
| --- | --- | --- |
| 1. 剧本解析 | 提取事件、人物关系、地点、道具与原文约束 | 拆解表、初步资产清单 |
| 2. 叙事分析 | 分析节拍、紧张度、心理和观众已知信息 | 叙事与心理分析 |
| 3. 中文导演稿确认 | 写逐镜场景、镜头运动、人物运动及必要创作依据，先确认中文拍法 | 已确认中文方案 |
| 4. 平面图与整场白模 | 依次确认布局和调度视频；白模可明确跳过 | 空间与行动依据 |
| 5. 最终分镜 | 依据白模核对中文稿，编译并确认系统分镜及对照，可选逐镜预演 | 可绑定实际参考的执行方案 |
| 6. 资产需求定稿 | 确定具体视角、状态与细节要求，复用已有资产 | 可执行生成清单 |
| 7. 基准设计与确认 | 确认风格和基础形象，执行家庭角色确认顺序 | 已确认基准图 |
| 8. 资产生成与检查 | 生成所需变体并校验外貌、布局和状态一致性 | 图片资产库 |
| 9. 打包交付 | 整理依赖、版本、用途和验证结果 | 可迁移资产包 |

第 4、5 阶段形成局部修订循环：镜头需要可以推动布局调整，布局限制也可以要求换机位。原文明示的空间与用户已确认布局优先；无法同时满足时说明冲突，不静默改动剧情或位置。未解决的问题应标为待验证，仅暂停依赖该问题的生成任务。

### 5.2 平面布局图能力

将平面布局图制作纳入 Skill 的正式能力，根据场景需求启用。以下情况默认建立布局：同一场景需要多个机位或角度、人物移动或多人调度、重要物件的可见性与遮挡影响剧情。简单背景或单一视角可省略，并记录判断理由。

每个场景保存一份基础布局，按镜头或镜头组叠加标注，避免把所有路线挤在一张图上。内容包括：

- 固定结构：墙体、门窗、开门方向、通道和相邻空间连接。
- 主要陈设：家具、关键道具的位置、朝向和占地范围。
- 人物调度：人物编号、起止位置、路线和事件节点。
- 摄影安排：机位编号、位置、朝向、运镜路线；有足够参数时绘制视场范围。
- 空间尺度：单位、必要尺寸、方向基准和比例说明。未知尺寸采用明确标记的设计假设，不伪装成实测数据。
- 光源说明：窗户与主要灯具位置；高度、光线等三维信息另外记录。

采用布局数据生成 SVG，并导出 PNG 预览。SVG 为可编辑矢量文件，文字和图例清晰可读；不以生图模型绘制的示意图片作为唯一空间依据。基础布局与镜头标注分层或分文件，布局变化后从同一份数据重新生成。

### 5.3 空间与运镜检查

- 人物是否能沿规划路线到达目标位置，门的开合是否受阻。
- 剧情要求“看见”或“看不见”的对象是否符合机位与人物视线；区分看到信封和读清信封文字。
- 摄像机是否有可用位置，路线是否穿墙或穿过实体；特殊摄影处理须明确记录。
- 镜头衔接的视线、运动方向、位置及持物状态是否连续；有意越轴应说明叙事目的和过渡方式。
- 动作与镜头时长是否相容，镜头是否承担过多动作或运动。
- 同一场景不同角度的门窗、家具、道具和主要光源是否保持对应关系。

平面图只能支持水平布局和基础路线判断。缺少高度、摄像机参数或物体形状时，遮挡与构图结论标为暂定，不声称已精确验证。

### 5.4 按需三维白模

楼梯、高低差、上下层关系、复杂遮挡或立体运镜影响镜头成立时，升级到三维白模。使用三维工具从布局数据建立真实可编辑的简化几何场景，设置摄像机位置、高度、朝向及必要的视场参数，输出所需机位或路径关键位置的检查预览。

交付包含可编辑三维源文件、与平面图一致的单位及坐标约定、摄像机标识和检查说明。首版通过 Python 直接导出自包含 glTF 2.0 网格和摄像机，并用 CPU 深度缓冲生成机位预览，不依赖本机安装 Blender。可在支持 glTF 的编辑器中继续编辑；外部编辑器导入效果尚未实测。

首版几何为旋转长方体近似，支持水平路径、三维线段碰撞与目标点遮挡检查。摄像机路径保存为元数据，不生成运镜动画；需要完整动态构图、门扇扫掠、曲面或角色骨骼验证时，仍需扩展工具或人工处理。导出成功不等于人工视觉检查通过。

生图工具生成的“白模风格图片”不能替代几何场景。所需三维工具不可用时，继续完成平面布局和独立资产工作，将相关镜头标为“三维验证待完成”，不自动宣称验证通过。

### 5.5 从空间与分镜生成资产需求

剧本解析仅建立初步清单，相关布局与分镜完成必要检查后才形成正式生成清单。人物设定等独立工作可以提前整理，依赖未定布局的场景图应等待布局稳定。

需求核对必须回答：这个镜头能否用现有资产说明人物身份、空间、道具外观和状态？若不足，需要新增哪项参考，以及为什么？

| 分镜需求 | 资产处理 |
| --- | --- |
| 面部近景 | 补充清晰面部参考；表情参考按需生成 |
| 人物背影 | 按需增加背面视角，保持服装结构一致 |
| 手部特写 | 明确左右手、皮肤、指甲、饰品和袖口；必要时生成细节图 |
| 信封特写 | 明确材质、尺寸关系、封口和可见文字 |
| 拆信前后 | 关联同一道具的密封与已拆封状态 |
| 场景反向视角 | 固定门窗、陈设和光源位置，再补视角 |
| 多机位的同一室内场景 | 引用同一布局版本，按需提供对应角度的空间参考，检查生成结果是否偏离布局 |

普通背景物件可以包含在场景中。只有情节关键、反复使用或需要特写的物件默认独立建档。并非每个文字分镜都需要一张新图片。

道具文字若原文未提供，不生成看似关键的姓名、地址或剧情线索。实际需要时采用无字设计、占位设计或询问关键内容，并明确标记。

## 6. 人物与家庭生成顺序

### 6.1 共同设定先于出图

先整理所有家庭成员的年龄关系、体态、服装、生活背景、相似与不同之处。家庭相似性是视觉设计目标，不承诺模拟真实遗传。

### 6.2 父母先确认，子女再生成

对于明确要求亲缘相似性的亲生家庭，默认执行：

1. 建立家庭整体设定。
2. 生成父母候选基准图，并检查画风、年龄和辨识度。
3. 展示父母基准图及外貌摘要，等待用户确认或修改。
4. 同时引用确认后的父母图，设计符合年龄的子女形象。
5. 展示家庭对照预览，检查相似性与独立辨识度，并等待用户确认。
6. 家庭基准确认后，才批量生成多视角、换装、表情和细节图。

子女不采用父母五官机械拼接，也不直接生成父母的缩小版本。若已有用户确认的子女形象，保留该形象，反向协调父母设计，不强制重做。

### 6.3 基准与变体

- 每个角色拥有独立编号和已确认基准版本。
- 外貌固定特征与服装、湿身、受伤等变化状态分开记录。
- 变体引用该角色基准图及必要的状态参考，避免仅重复文字提示词。
- 后续参考优先包含基准图，避免只沿上一张变体连续编辑而累积偏差。
- 多视角预览可用于审核；供后续使用的默认文件为单视角、无标注图片。

## 7. 确认节点与持续执行

确认应集中在会影响大量后续资产的节点，展示具体图片及待确认设定，而不是要求用户批准抽象计划。

| 节点 | 默认行为 |
| --- | --- |
| 剧本分析、文字分镜 | 自动完成并标明推断与建议；剧情关键歧义单独提问 |
| 平面布局与白模检查 | 在原文和已确认设定范围内自动执行，不增加逐图确认；影响剧情或已确认设计的冲突需说明并取得决定 |
| 全局视觉风格与代表样图 | 在批量生成前确认；若已有明确确认参考则复用 |
| 父母基准形象 | 等待确认后生成子女 |
| 家庭整体基准 | 等待确认后扩展角色资产 |
| 基准下的普通变体 | 在已确认范围内持续生成与检查，无需逐图确认 |
| 改变已确认外貌或剧情 | 展示影响范围，取得相应决定后执行 |

可以合并重叠节点，例如父母样图同时承担全局风格确认。等待用户时允许继续独立的文字分析和资产清单整理，不提前生成依赖待确认形象的资产。

## 8. 技术组成

| 组件 | 责任 |
| --- | --- |
|助手与 `SKILL.md` | 理解任务、调用工具、执行流程和维护状态 |
| 按需读取的参考规范 | 分析字段、分镜规则、家庭设计和资产数据约定 |
| 当前可用的图像生成与编辑工具 | 创建基准图、参考变体和细节图 |
| 布局数据与 SVG 绘图、渲染工具 | 绘制基础平面图和分镜标注，导出 PNG 预览 |
| Python glTF 导出与 CPU 深度缓冲渲染 | 按需建立白模、设置机位、输出空间检查预览 |
| Python 辅助脚本 | 校验数据与文件、检查引用、生成预览索引、打包 |

Skill 本身是工作指令与资源集合，实际出图依赖运行环境可调用的工具。第一版优先使用当前环境已有的图像生成能力；不把未经确认的底层模型名称写成固定依赖。

两台电脑的分工：开发机运行 助手、Python 虚拟环境、平面图与 glTF 导出；用户已说明 Blender 位于运行 H3 的另一台电脑，该机器承担白模导入、进一步编辑与视频制作。静态功能不要求开发机安装 Blender；用户选择本地动态预演时使用本机 Blender。若未来需要开发机执行 Blender 专有操作或运镜动画验证，再选择本机安装或远程调用；当前未建立远程连接，Blender 实际导入也尚未测试。

工具不可用或未返回可保存图片时，保留分析与生成请求，并明确标记缺失项，不能把提示词当作已生成图片。独立运行的图像 API 适配可后续增加，届时需验证凭据、接口、输出保存和重试行为。

建议的 Skill 结构：

```text
script-visual-director/
├── SKILL.md
├── agents/openai.yaml
├── references/
│   ├── narrative-and-camera.md
│   ├── spatial-planning.md
│   ├── blockout.md
│   ├── family-design.md
│   ├── image-production.md
│   ├── asset-schema.md
│   └── h3-handoff.md
├── scripts/
│   ├── svd.py                # 统一 CLI 入口
│   ├── core.py               # 资产、确认、版本、校验和打包
│   ├── schema.py             # 精确数据结构
│   ├── layout.py             # 平面图与空间检查
│   └── blockout.py           # 三维几何与机位预览
├── examples/                 # 可运行的规划示例
├── tests/                    # 行为与文件验收测试
├── pyproject.toml
└── requirements.lock
```

入口文件只保留适用范围、核心流程、确认条件和资源导航。复杂规则放入相关参考文件，执行对应阶段时再读取。脚本仅承担重复且可确定验证的操作。

## 9. 数据、状态与可追踪性

### 9.1 关联关系

`原文段落 → 剧情节拍 → 文字分镜 ↔ 场景布局／机位／可选白模 → 资产需求 → 资产版本 → 图片文件`

使用稳定编号，例如 `BEAT_001`、`SHOT_001`、`CHAR_001`、`LOC_001`、`PROP_001`。展示名称可以修改，编号不随名称变化。

### 9.2 资产索引

每个资产至少记录：编号、类型、名称、设定、原文依据、关联镜头、固定特征、视角或状态、基础资产、当前版本、相对文件路径、参考图、生成记录和检查结果。

参考图编号不等于 H3 输入序号。实际导入某次生成任务时，应由下游工作流建立选图顺序和用途映射。

### 9.3 状态分开记录

- 生产状态：`planned`、`prompt_ready`、`generated`、`failed`。
- 检查状态：`pending`、`passed`、`needs_revision`。
- 确认状态：`not_required`、`pending`、`approved`、`rejected`。
- 有效性：`current`、`stale`、`superseded`。

“已生成”“已检查”和“用户已确认”分别表达不同事实。自动视觉检查不能代替要求用户确认的基准节点。

### 9.4 空间数据与验证记录

每个布局记录场景编号、布局版本、单位、原点与坐标方向、结构、陈设、关键物件和设计假设。各镜头引用对应布局版本，单独记录机位、人物路径、道具状态及必要的高度和视场参数。存在移动陈设时记录状态变化，不把不同镜头的临时位置写回同一静态布局。

白模记录来源布局版本、三维工具与源文件、机位标识、预览文件及检查范围。空间验证状态采用 `not_required`、`pending`、`passed`、`needs_revision`；`passed` 仅代表记录范围内的检查通过，不保证下游生图或视频会严格遵循几何布局。

布局数据是平面图、白模及空间说明的共同依据。生成的场景图仍需单独比对门窗、陈设和主要光源，不因参考了平面图就视为一致。

## 10. 交付结构与跨电脑使用

建议交付：

```text
project-assets/
├── manifest.json
├── project-settings.json
├── analysis/
│   ├── narrative-analysis.md
│   ├── character-analysis.md
│   └── camera-plan.md
├── visual-guide.md
├── spatial/                      # 有布局需求时生成
│   └── LOC_001/
│       ├── layout.json
│       ├── floor-plan.svg
│       ├── floor-plan.png
│       ├── camera-overlays/      # 各镜头的机位与走位标注
│       ├── validation.md
│       └── blockout/             # 启用白模时保存三维源文件和检查预览
├── characters/
├── scenes/
├── props/
├── previews/
├── generation-records.json
└── handoff.md
```

`handoff.md` 说明资产用途、已确认版本、未完成项目和导入方式是否经过验证。预览图与可投喂原图分目录保存。

平面图、白模及其机位预览属于空间规划资料，与角色、场景、道具视觉参考图分开交付，并在索引中标注用途。默认不将标注图或白模渲染图混入 H3 参考图列表；只有目标工作流实际验证适用后才纳入。未启用空间模块时不生成空目录或虚假的验证文件。

打包使用相对路径，不依赖本机绝对路径或临时下载地址。文件名使用稳定编号和版本，中文名称保存在索引中。图片格式、尺寸与文件可读性在打包前检查；默认保存可用原图，目标工作流需要的格式转换另生成副本，不覆盖原文件。

`manifest.json` 是本项目自定义资产清单，MiniMax-H3 不会自动识别这个格式。第一版通过手动选图与文本说明交接；自动适配需根据实际 ComfyUI 工作流或 Python/API 接口实现。

## 11. MiniMax-H3 对接约束

根据 2026-09-14 查阅的官方模型说明：

- `H3-Base-Ref2VA` 用于多模态参考输入，官方列出的图片上限为 9 张。
- `H3-Base-FL2VA` 用于文生视频、首帧、尾帧或首尾帧模式。

当前项目的独立角色、场景、道具参考图，更适合 Ref2VA 的输入思路。资产库总量不受单次参考图数量限制；下游每次生成应按任务选图，并核对其部署实现的实际限制。

若用户只部署 FL2VA，需要另行确定下游如何制作或提供具体首尾帧，不把整个资产包声称为可直接导入。

本 Skill 不生成视频任务，因此文字分镜不等于完整的 H3 推理提示词或官方 Context-IR 输出，也不承诺仅凭资产包就获得官方完整工作流的效果。

## 12. 局部修改与异常处理

- 修改原文后，识别受影响的节拍、分镜和资产，不重新生成全部文件。
- 修改门窗、家具布局或人物路线后，更新布局版本，重新导出相关平面图与标注；关联白模、空间检查和场景图片标为待复核。只有受影响的机位和资产需要更新。
- 修改人物基准后，将其变体标为待复核；父母基准变化时，子女形象也进入关联复核，但不直接覆盖已确认子女。
- 更换服装只影响相应造型及其关联需求，不自动改变角色身份特征。
- 所有再生成保留版本关系和原因，交付索引指向有效版本。
- 生成失败时记录错误和可用结果，按用户预算与运行工具规则进行有限重试。第一版建议每项最多自动重试一次；连续无改善、预算用尽或必须改变已确认设定时暂停相关资产，继续独立工作。
- 接口超时、结果不确定时先核对已有文件或任务状态，避免盲目重复提交。

## 13. 第一版验收标准

### 13.1 示例：简短日常描述

输入：“一个女人晚上回到家，发现桌上有一封信。”

期望：自动给出有叙事目的的文字分镜；不擅自添加危险和寄信人身份；从手部及信封特写推导必要资产；不产出分镜首尾帧或视频。

### 13.2 示例：亲生家庭角色

输入包含爸爸、妈妈、儿子，并明确要求亲缘相似性。

期望：先设计父母并展示确认；确认之前不生成依赖父母外貌的子女图；确认后用父母参考生成儿子；家庭基准确认前不批量扩展个人变体。

### 13.3 示例：局部修改

将密封信封改为已经拆开，其他内容不变。

期望：更新相关道具状态和镜头衔接，保留无关角色和场景资产，不重新生成整套图片。

### 13.4 示例：多机位室内场景

输入包含从门口进入、发现桌上信封、走到桌前，以及从另一方向拍摄人物的要求。

期望：生成可编辑 SVG 平面图、PNG 预览和布局数据；门窗、桌面、信封、人物路线和机位编号对应一致。检查门口是否能看见信封以及路线是否被陈设阻挡。更改桌子位置后，相关机位标注与场景资产进入复核，无关人物基准不受影响。

### 13.5 示例：复杂空间与工具不可用

输入涉及人物上楼、栏杆遮挡和摄像机升高跟随。

期望：识别平面图不能充分验证高度与遮挡，触发白模需求。有三维工具时输出可编辑几何场景和检查预览；工具不可用时保留待验证状态，不能用白模风格图片代替验证结果。简单单视角背景则可以跳过布局与白模，并记录理由。

### 13.6 文件验收

- 每个计划生成的资产能追溯到原文或具体镜头需求。
- 已生成记录存在对应的可读图片，索引无断链、编号无冲突。
- 基准、变体、参考来源和确认状态清楚。
- 布局、文字分镜、摄像机与白模引用的版本一致；SVG／PNG 标注可读且对应布局数据。
- 可编辑白模确有几何场景与摄像机，不只是渲染图片；工具缺失时验证状态真实反映未完成项。
- ZIP 解压到新目录后，所有相对文件引用仍能解析。
- 未通过目标 H3 环境实测时，交付说明明确标注“通用资产包，目标导入待验证”。

视觉验收同时检查人物身份、年龄、服装、手部、道具状态和场景空间关系，不仅检查文件格式。

## 14. 实施顺序与待确定事项

第一阶段：实现分析、文字分镜、平面布局数据与 SVG／PNG 输出、空间检查、资产清单与数据校验，用简短剧本和多机位室内片段验证流程。  
第二阶段：接入图像生成，完成基准确认、家庭设计、变体和视觉检查；实现按需白模模块并核验三维工具能力。平面图无法解决的镜头在模块可用前明确保持待验证。  
第三阶段：实现布局及资产版本追踪、局部更新和跨电脑打包。  
第四阶段：获得另一台电脑的工作流信息后，按需要增加导入适配，并验证是否采用空间参考资料。

待确定事项：

1. Skill 源文件位于 `/Users/cuipeng/Work/Project/script-visual-director`；个人发现目录 `/Users/cuipeng/.codex/skills/script-visual-director` 已通过链接指向项目。此项已落实。
2. 第一轮样例剧本、视觉风格和已有参考图。
3. 另一台电脑使用的 H3 变体及 ComfyUI 或 Python/API 运行方式。
4. 首轮资产数量、候选图数量和生成预算偏好。
5. 若需要超出长方体近似和静态机位的验证，再确定外部三维编辑器及动态预演环境；当前平面图与 glTF 白模不依赖该项决定。

以上信息按相关阶段补齐，不阻塞当前设计文档和通用数据结构的准备。

## 15. 参考资料

- [MiniMax-H3 官方模型说明](https://huggingface.co/MiniMaxAI/MiniMax-H3)：模型变体、参考输入限制与本地部署说明。
- [OpenAI 图像生成文档](https://developers.openai.com/api/docs/guides/image-generation)：图像生成、编辑和参考图输入能力。当前环境工具能力以实际可调用接口为准。

外部能力和输入限制可能变化，实现具体适配前应再次核对目标版本。


## v0.2 补充：交互式分镜审核与阶段确认

2026-09-14：采用“中文编辑源稿 + 目标系统适配稿”模式。目标系统不限于 MiniMax-H3；参考图片制作工具独立配置。先剧情分析确认，再目标格式分镜/中文对照确认，再平面图确认，再必要白模及对照预演确认，之后生产基准和扩展资产。

第一版已实现 Python 标准库本地服务及浏览器页面，编辑结构化中文、对照目标文本、查看版本差异、逐镜/阶段确认和预演播放。页面保存后由用户在当前对话说“同步修改”，skill 导出请求、人工编译适配稿和检查依赖，再通过带版本校验的结果写回。页面不自动调用模型，也不安装后台常驻服务。

采用 review/state.json 持久化，保留修改前快照、确认依据、系统配置和内容指纹。HTTP 与 CLI 共享文件锁，项目 revision 防止并发覆盖。中文修改保留旧系统文本，撤销当前镜头及下游阶段确认；模型切换保持中文，要求重编译系统文本。影响候选由关系计算，失效清单由助手审核，沿资产依赖传播。已启用审核的项目在前置阶段完成之前禁止排队生产资产。

原 v0.3 不实现自动调用模型与 Blender 动态预演；当前本地预演流程按下述更新执行，最终 AI 视频仍不在范围内。对照预演为审核资料，可登记并播放另一台 Blender 机器的真实视频；没有视频不能把静态机位 PNG 标成预演已完成。审核服务仅绑定本机回环地址，需随机令牌，无模型 API 密钥，文件服务仅提供项目登记资料。

具体数据与命令以 references/interactive-review.md 为准。原 camera-plan.md 导入后作为历史稿；当前结构化分镜和时长以审核状态为准。旧 v0.1 项目保持兼容，但按新流程使用时先显式导入审核状态，已有图片不代表前置阶段已获用户确认。


## v0.2.1 补充：完整中文对照与人物四图

目标系统原文增加独立的完整中文对照 translation_zh，与英文共同写入、共同参与版本确认，不能用左侧简短导演摘要代替。旧项目缺少完整对照时要求补齐再确认。

人物默认输出四张独立文件：颈部以下正视、含头部背视、含头部左侧视、头部特写。四图共用同一身份基准，统一脸型、服装、体型和材质状态；裁去头部的正视是颈部下方的正常取景裁切。四图分别登记依赖与版本，不用四宫格代替。详细要求见 references/character-reference.md。

## v0.3 节奏、人物与逐句语气

导演稿新增心理、可见表演、逐句语气、节奏字段。叙事节拍跟踪信息差与角色选择；角色分析覆盖目标、认知、策略、外在呈现与内在冲突。每句台词保留原文，同时标注语气及必要的语速、停顿、重音和音量。

审核拆成字段完整性、目标文本同步、助手 全段与逐镜导演检查、用户确认。新增 review-check、review-director、review-edit 和完整中文初始化输入。导演检查绑定全段内容及原文/分析指纹；上下文变化使旧检查过期。它不自动评分艺术质量，也不生产配音。详细协议以 references/director-review.md 为准。


## v0.4 本地可选预演

布局确认 → 询问生成或跳过 → 生成时检测本机 Blender、按剧情准备动画适配、后台 smoke、完整渲染、实际视频检查、用户确认 → 基准资产。用户可跳过，即使存在复杂走位；记录当前决定及未验证项，解除本轮白模依赖，保留 pending 检查，不伪造通过。

新增 previs-doctor/choice/bind/run/check 命令；支持现有项目适配脚本，按分镜、布局和输入内容绑定版本。工具不自动推断走位；助手 负责本剧动画设计。输出 MP4/H.264、.blend 与 PNG 序列；缺帧、旧输入或覆盖不全不能确认。Blender 在独立后台进程运行，页面只显示状态与结果链接，不自动展示或播放。详细约定见 references/local-previs.md。


## v0.5 逐镜预演与空间约束资产

预演交付按镜号一一对应：每镜审核 MP4、纯摄影机参考候选 MP4，再输出原顺序串联版。新任务使用 per_shot_v1 协议，原始 comparison/camera 帧序列共享镜号、时间与场景状态；导出验证视频元数据和源／产物指纹，用户确认独立于导出。部分覆盖与完整分镜分开报告，禁止补造缺镜。协议见 references/local-previs.md。

预演确认后生成资产。人物四张独立图不变；背景依据预演方向、机位高度、视场和空间几何生成，记录布局版本、预演镜号与帧、资产基准版本的映射。生成工具做美术与材质细化，空间检查维护门窗、柜台、家具和通行关系。场景流程见 references/scene-reference.md。H3 具体部署的限长细分与实际推理仍须验证。


## v0.6 通用生成分组与 AIMixer 适配

新增 groups/state.json 与独立历史：分组保存、同步、确认采用源分镜指纹及双版本校验。镜头按原顺序完整覆盖，组内时间从0开始；用户在网页修改导演意图并由助手编译中英稿。分组确认不代表素材已生成。

逐镜预演可按组输出审核与纯摄影机串联版，有缺镜的组列为待办；文件与源索引指纹用于阻止过期引用。公共／组内素材保留通用 key 和确切资产版本，适配器编译为槽位。当前提供 generic ZIP 与 AIMixer r2v mmxpack，按真实上游导入器进行契约验证，仍保留 target_h3_validated=false。完整操作见 references/generation-groups.md。


## v0.7：导演组与电影化二次创作

导演组是一次合并输出的完整段落，可53秒、48秒或更长。内部多个视频段分别生成；H3参考输入预算只检查视频段任务。旧 groups 数据字段保留为任务列表，新增 director_groups 存储导演组及任务顺序。旧项目不自动改写，补充分组方案后重新确认；不重做或覆盖原始图片。

先撰写可读的 creative-treatment.md，在保持核心设定的前提下主动丰富生活细节、潜台词、反应、画外声和揭示顺序，可选择“局部逐步揭示整体”；每个细节有作用，不按特写数或15秒节拍写戏。具体见 references/creative-development.md。

`groups-package PROJECT OUTPUT.mmxpack.zip --director-group D01` 导出一个导演组；`groups-bundle PROJECT OUTPUT.zip` 导出包含多个独立导演包的总ZIP。解压后每包分别导入、生成并合并，通用适配器可手动逐段生成和剪辑。本skill不生成最终AI视频。创作时长和模型帧对齐后的时长分别报告。

网页展示两层结构及合并时长。预演按任务提供短参考视频，按导演组提供整体节奏审核片；不能把整组长预演当作单次H3输入。详见 references/generation-groups.md。


## v0.8：剧集共享资产与第二集继承

剧集共享库保存已完成图片的不可覆盖版本，每集独立保存剧本、导演组、布局、预演和确认。第二集开始先核对上一集结束状态，列出直接复用、参考改造、新增三类资产；人物四图、服装、道具状态分别选定，场景结合本集布局检查。

使用 series-init 建库、series-publish 入库、series-list 查看确切版本、series-inherit 按方案复制到第二集、series-check 验证来源和图片哈希。库版本不自动更新各集；已审核图不会被覆盖。参考候选和继承场景须本集复核，同图人物／道具可保留原有图像批准。项目页显示来源集和版本，正式导出校验继承文件，迁移第二集不依赖第一集绝对路径。

完整命令和JSON方案见 [跨集资产继承](references/series-continuity.md)。


## 九宫格交互审核实现（2026-09-17）

新增 scripts/grid_workflow.py、scripts/grid_server.py 和 assets/grid-review/。独立grid-workflow.json保存director、characters、grids、prompts四阶段的文件哈希、批准及历史；不修改旧review/state.json。网页提供导演稿文本编辑、图片查看、只读提示词与中文对照、修改意见及阶段确认。CLI与HTTP共用文件锁，网页写入按当前状态和文件内容生成的revision拒绝过期请求。重复确认有效版本不使下游失效；上游修改或待修改意见阻止继续使用旧批准。

服务绑定127.0.0.1，使用随机token、Host／Origin检查和登记文件白名单。页面每3秒检查变化，有未保存输入时不覆盖；确认和保存不会自动调用模型。新模式的九宫格视觉质量、身份和剧情连续性仍由助手实际检查；程序不自动证明画面正确。导演台原生包自动导出尚未适配，当前交付文件与素材映射供手动导入。未完成目标H3的视频实测。

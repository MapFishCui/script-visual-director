# 剧本视觉导演：当前设计

当前默认完整制作流程为中文导演稿 → 人物基准与四视图 → 按任务制作九宫格 → 提示词与中文对照 → 校验和交付。单项参考图、提示词和情节分镜仍按请求范围直接交付，不强制进入完整项目。

## 数据与职责

- 助手负责理解情节、导演创作、真实图像生成和视觉审阅。
- `grid-workflow.json` 保存文件指纹、版本、用户确认与操作历史；旧整体登记保持兼容，新项目可按人物和任务记录。
- `grid-delivery.json` 保存本集人物、版本、任务、素材路径和槽位映射，与导演稿一起确认。格式见 [交付协议](references/grid-delivery.md)。
- `grid_items.py` 将人物绑定导演稿，将九宫格绑定实际使用的人物，将提示词绑定同编号九宫格。文件或登记版本变动使相关依赖过期，无关任务保持确认。导演稿和任务规划变动仍保守影响全项目。
- `grid_delivery.py` 独立检查材料完整性并导出通用 ZIP 或 AIMixer 格式包，不调用旧布局关卡。导出包含已登记原件、恢复状态、任务文本、素材映射和哈希。
- `grid_series.py` 通过现有 series 入口存取不可覆盖的完整人物套件；旧单图版本和新套件版本明确区分。

## 审阅与恢复

网页以当前状态和文件内容生成 revision，拒绝过期写入；用户草稿单独保留，遇到新正文显示对照并要求明确完成合并。清空意见、恢复原文或撤销编辑会移除未保存标记。轮询旧响应不能覆盖后续保存结果。

用户确认和交付完整性分别判断：确认不证明素材齐备；结构校验也不证明九宫格视觉或导演质量合格。现有阶段历史保存状态，不是所有图片的自动版本备份，制作时仍须另存修订文件。

## 兼容性与已知边界

原 manifest/review 项目沿用原协议，不自动迁移、不补造批准。已整体登记的九宫格阶段不自动改成逐项模式。新流程不强制平面图和白模。

新导演台格式包沿用仓库现有适配器契约；未运行真实目标采样，始终保留 target_h3_validated=false。新流程的场景／道具跨集自动入库、自动判断九宫格人物一致性和真实视频效果仍未实现。

当前功能表见 [操作入口](README.md)。旧设计、版本沿革和当时的环境测试记录保留在 [历史设计记录](references/design-history.md)，不作为当前命令指南。
